// // Pak het template element
// const template = document.getElementById("navbar-template");

// // Controleer of het template bestaat
// if (template) {
//     // Clone de inhoud van het template
//     const clone = template.content.cloneNode(true);

//     // Plaats de clone in de placeholder
//     const navbarPlaceholder = document.getElementById("navbar");
//     if (navbarPlaceholder) {
//         navbarPlaceholder.appendChild(clone);
//     } else {
//         console.error("Navbar placeholder niet gevonden!");
//     }
// } else {
//     console.error("Navbar template niet gevonden!");
// }
